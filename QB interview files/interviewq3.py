def get_gradient(X, y, y_pred):
    """
    =========== TO FILL =============
    
    Computes the gradient of the MSE loss for each weight
    
    Args:
        - X(N x d np.ndarray): N samples each with d features
        - y(N x 1 np.ndarray): targets (between -1 and 1)
        - y_pred(N x 1 np.ndarray): vector of tanh model predictions (between -1 and 1)
    
    Returns:
        (1 x d np.ndarray) gradient for each weight
    """
    
    m = len(y)
    
    cost
    return     
    #TODO: replace Error below by return statement
    # raise NotImplementedError('Gradient calculation function not implemented')

        
        
        

y = [1, -1, 1, -1 ]
y_pred =  [0.5, -0.5, 0.99, 0.99]